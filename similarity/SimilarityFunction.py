import pandas as pd


class SimilarityFunction:
    def computeSimilarity(self, queryRepresentation: pd.DataFrame, documentRepresentation: pd.DataFrame):
        """Computes the similarity between the `queryRepresentation` and the `documentRepresenation`. Implementation is done in child classes."""
        pass